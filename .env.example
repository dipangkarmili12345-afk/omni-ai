# Omni AI Security Architecture:
# ZERO AI provider API keys exist inside the Android application.
# All AI provider keys (GEMINI_API_KEY, OPENAI_API_KEY, ANTHROPIC_API_KEY, DEEPSEEK_API_KEY, XAI_API_KEY)
# are securely configured server-side in Firebase Cloud Functions / Secret Manager.

